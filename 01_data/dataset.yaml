# Barcode Detection SDK — 数据集配置
# Ultralytics YOLO 格式

path: ../dataset    # 数据集根目录（训练时修改为实际路径）
train: train/       # 训练集（相对 path）
val: test/          # 验证集
test: test/         # 测试集

# 类别定义
names:
  0: barcode
  1: fp

# 注：数据集经过预标注系统 + 人工矫正后生成
